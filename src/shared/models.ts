export enum AlertTypes {
  ERROR = "error",
  SUCCESS = "success",
  ALERT = "alert",
}

export enum UserRoles {
  TEACHER = "teacher",
  STUDENT = "student",
}

export enum MessageTypes {
  ERROR = "error",
  HIGHLIGHTED = "highlighted",
  DEFAULT = "default",
}